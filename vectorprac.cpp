#include<iostream>
#include<vector>
using namespace std;
int main()
{
   /* int i,n=5;
    vector<int> one;
    vector<int> two;
    vector<int> sum;
    for(i=0;i<n;i++)
    {
        int temp;
        cout<<"Enter1";
        cin>>temp;
        one.push_back(temp);
    }
    for(i=0;i<n;i++)
    {
        int temp1;
        cout<<"Enter2";
        cin>>temp1;
        two.push_back(temp1);
    }
     for(i=0;i<n;i++)
    {
        sum.push_back(one[i]+two[i]);
    }

    for(auto element:sum)
    {
        cout<<element<<endl;
    }*/

















}
