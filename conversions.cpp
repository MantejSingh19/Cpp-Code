#include<bits/stdc++.h>
using namespace std;
int main()
{
    //conversion from decimal to binary
    /*int n,i;
    cin>>n;
    while(n>0)
    {
        i=n%2;
        cout<<i<<endl;
        n=n/2;
    }*/
    //conversion from binary to decimal
   /* long int n;
   int i,sum=0,c;
    cin>>n;
    for(i=0;i<=3;i++)
    {
        c=n%10;
        n=n/10;
        if(c==0)
        {

            continue;
        }
        sum=sum+pow(2,i);

    }
    cout<<sum;*/
    //hexadecimal to decimal

 /*string c;
 int i,j,sum=0,x=1;
 cin>>c;
 j=c.size();
 for(i=j-1;i>=0;i--)
 {
     if(c[i]>='0' && c[i]<='9')
     {
         sum=sum+(x*(c[i]-'0'));
     }
     else if(c[i]>='A' && c[i]<='F')
     {
         sum=sum+(((c[i]-'A')+ 10)*x);
     }
     x=x*16;
 }
 cout<<sum;*/









































}
