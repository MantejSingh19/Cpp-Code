#include<iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int arr[n],i,j;
    for(i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    /*lf=0;
    rg=n-1;
    mid=(lf+rg)/2;
    while(lf<=rg)
    {
        mid=(lf+rg)/2;
        if(key==arr[mid])
        {
            cout<<mid;
            break;
        }
      else if(key>arr[mid])
        {
            lf=mid+1;
        }
    else if(key<arr[mid])
    {
        rg=mid-1;
    }

    }
    cout<<"Element not found";

    int minn=INT_MAX,temp;
    for(i=0;i<n;i++)
    {
        int minn=INT_MAX;
       for(j=i;j<n;j++)
       {
            minn=min(minn,arr[j]);

       }
        for(j=i;j<n;j++)
       {
            if(arr[j]==minn)
            {
                break;
            }

       }
       temp=arr[i];
       arr[i]=minn;
       arr[j]=temp;
    }
     for(i=0;i<n;i++)
    {
        cout<<arr[i];
    }

int temp;
    for(i=0;i<n-1;i++)
    {
        for(j=0;j<n-i;j++)
        {
            if(arr[j]>arr[j+1])
            {
                temp=arr[j];
                arr[j]=arr[j+1];
                arr[j+1]=temp;
            }
        }
    }
    for(i=0;i<n;i++)
    {
        cout<<arr[i];
    }*/





































}
